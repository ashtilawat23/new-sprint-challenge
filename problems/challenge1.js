/*
  CHALLENGE 1: Concatenator Function

  The "concatenator" function is designed to handle two arguments, `a` and `b`, 
  which can be either numbers or strings.

  Requirements:
  - If `a` and `b` are strings, the function should return them concatenated.
  - If `a` and `b` are numbers, the function should return their sum.
  - The function must validate that `a` and `b` are of the same type. 
    If not, return the message "a and b must be of the same type".
  - The function must check if `a` and `b` are either numbers or strings. 
    If not, return "a and b must be numbers or strings".
  - Edge cases should be considered and handled appropriately.

  Test Cases:
  1. concatenator(1, 'a') should return "a and b must be of the same type"
  2. concatenator('a', 1) should return "a and b must be of the same type"
  3. concatenator(true, false) should return "a and b must be numbers or strings"
  4. concatenator(null, null) should return "a and b must be numbers or strings"
  5. concatenator('toyota', ' camry') should return "toyota camry"
  6. concatenator(3, 5) should return 8
  7. concatenator(3, -5) should return -2

  Additional Information:
  - The order of conditions in the implementation may affect the correctness 
    and efficiency of your function.
  - Consider handling edge cases before processing the main logic to make the 
    function robust.
*/

function concatenator(a, b) {
    // Your code here
  }
  